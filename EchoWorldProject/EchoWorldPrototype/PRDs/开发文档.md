# ECHOWORLD 开发文档

**界面功能实现与技术要求**

---

## 目录

- [一、技术栈选择](#一技术栈选择)
- [二、项目结构](#二项目结构)
- [三、核心功能模块](#三核心功能模块)
- [四、世界地图界面](#四世界地图界面)
- [五、对话界面](#五对话界面)
- [六、角色状态界面](#六角色状态界面)
- [七、时间线界面](#七时间线界面)
- [八、设置界面](#八设置界面)
- [九、角色创建流程](#九角色创建流程)
- [十、数据模型设计](#十数据模型设计)
- [十一、API接口设计](#十一api接口设计)
- [十二、性能优化](#十二性能优化)
- [十三、测试策略](#十三测试策略)

---

## 一、技术栈选择

### 1.1 前端框架
- **React Native**: 跨平台移动应用开发
- **TypeScript**: 类型安全的JavaScript
- **React Navigation**: 导航解决方案
- **React Native Reanimated**: 高性能动画

### 1.2 状态管理
- **Zustand**: 轻量级状态管理
- **React Query**: 服务端状态管理
- **AsyncStorage**: 本地数据存储

### 1.3 UI组件库
- **React Native Elements**: 基础UI组件
- **React Native Vector Icons**: 图标库
- **React Native Maps**: 地图组件

### 1.4 开发工具
- **Expo**: 开发环境与构建工具
- **ESLint + Prettier**: 代码规范
- **Jest + Testing Library**: 测试框架

---

## 二、项目结构

```
src/
├── components/          # 可复用组件
│   ├── common/         # 通用组件
│   ├── ui/             # UI基础组件
│   └── layout/         # 布局组件
├── screens/            # 界面组件
│   ├── World/          # 世界地图界面
│   ├── Chat/           # 对话界面
│   ├── Profile/        # 角色状态界面
│   ├── Timeline/       # 时间线界面
│   └── Settings/       # 设置界面
├── navigation/         # 导航配置
├── services/           # API服务层
├── stores/             # 状态管理
├── utils/              # 工具函数
├── types/              # TypeScript类型定义
└── assets/             # 静态资源
```

---

## 三、核心功能模块

### 3.1 角色管理系统
- 角色创建、状态管理、行为引擎
- 情绪系统、精力系统、记忆系统

### 3.2 对话系统
- AI对话引擎、消息管理、上下文处理
- 延迟回复机制、性格化回复

### 3.3 时间线系统
- 动态发布、互动功能、内容管理
- 推送通知、缺席感知

### 3.4 地图系统
- 角色位置显示、地标管理
- 缩放控制、交互逻辑

---

## 四、世界地图界面

### 4.1 组件结构
```javascript
// WorldScreen.js
import React, { useEffect, useState, useRef } from 'react';
import { StyleSheet, View, Text, TouchableOpacity, Modal, Image, PanResponder, Animated } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { useWorldStore } from '../../stores/worldStore';

export default function WorldScreen({ navigation }) {
  const { characters, locations, fetchCharacters, fetchLocations } = useWorldStore();
  const [selectedCharacter, setSelectedCharacter] = useState(null);
  const [modalVisible, setModalVisible] = useState(false);
  
  // 双指拖拽和缩放状态
  const translateX = useRef(new Animated.Value(0)).current;
  const translateY = useRef(new Animated.Value(0)).current;
  const scale = useRef(new Animated.Value(1)).current;
  const baseScale = useRef(1);
  const baseTranslateX = useRef(0);
  const baseTranslateY = useRef(0);
  
  // PanResponder配置
  const panResponder = useRef(
    PanResponder.create({
      onStartShouldSetPanResponder: () => true,
      onMoveShouldSetPanResponder: () => true,
      onPanResponderGrant: (_, gestureState) => {
        if (gestureState.numberActiveTouches === 2) {
          // 双指缩放
          const dx = gestureState.locationX[0] - gestureState.locationX[1];
          const dy = gestureState.locationY[0] - gestureState.locationY[1];
          baseScale.current = scale.__getValue();
          baseTranslateX.current = translateX.__getValue();
          baseTranslateY.current = translateY.__getValue();
        } else if (gestureState.numberActiveTouches === 1) {
          // 单指拖拽
          baseTranslateX.current = translateX.__getValue();
          baseTranslateY.current = translateY.__getValue();
        }
      },
      onPanResponderMove: (_, gestureState) => {
        if (gestureState.numberActiveTouches === 2) {
          // 双指缩放和拖拽
          const dx = gestureState.locationX[0] - gestureState.locationX[1];
          const dy = gestureState.locationY[0] - gestureState.locationY[1];
          const distance = Math.sqrt(dx * dx + dy * dy);
          const newScale = (distance / initialDistance.current) * baseScale.current;
          const boundedScale = Math.min(Math.max(newScale, 1), 3);
          
          scale.setValue(boundedScale);
          translateX.setValue(newTranslateX);
          translateY.setValue(newTranslateY);
        } else if (gestureState.numberActiveTouches === 1) {
          // 单指拖拽
          const deltaX = gestureState.dx;
          const deltaY = gestureState.dy;
          translateX.setValue(baseTranslateX.current + deltaX);
          translateY.setValue(baseTranslateY.current + deltaY);
        }
      },
    })
  ).current;
  
  return (
    <SafeAreaView style={styles.container}>
      <View style={styles.mapContainer}>
        <Animated.View
          style={[
            styles.mapPlaceholder,
            {
              transform: [
                { translateX },
                { translateY },
                { scale }
              ]
            }
          ]}
          {...panResponder.panHandlers}
        >
          <Text style={styles.mapText}>世界地图</Text>
          {characters && characters.map((character) => (
            <TouchableOpacity
              key={character.id}
              style={[
                styles.characterMarker,
                {
                  left: character.currentLocation.longitude - 116.4 + 150,
                  top: 39.9 - character.currentLocation.latitude + 100
                }
              ]}
              onPress={() => handleCharacterPress(character)}
            >
              <Image
                source={{ uri: character.avatar }}
                style={styles.characterAvatar}
              />
              {character.status === 'active' && <View style={styles.activeIndicator} />}
            </TouchableOpacity>
          ))}
        </Animated.View>
      </View>
    </SafeAreaView>
  );
}
```

### 4.2 功能实现要点
- **双指操作**: 支持双指缩放（范围1-3倍）和双指拖拽
- **单指拖拽**: 支持单指拖动地图
- **标记渲染**: 根据角色状态（在线/离线）动态更新标记样式
- **交互处理**: 点击角色标记弹出信息卡片，支持发消息和查看详情
- **动画效果**: 使用Animated API实现平滑的拖拽和缩放动画
- **安全检查**: 添加了对characters数组的安全访问检查

### 4.3 数据流管理
```typescript
// 世界地图状态管理
interface WorldState {
  characters: Character[];
  locations: Location[];
  mapRegion: Region;
  selectedCharacter: Character | null;
  loading: boolean;
}

const useWorldStore = create<WorldState>((set, get) => ({
  characters: [],
  locations: [],
  mapRegion: initialRegion,
  selectedCharacter: null,
  loading: false,
  
  // 动作
  fetchCharacters: async () => {
    set({ loading: true });
    const characters = await CharacterService.getAll();
    set({ characters, loading: false });
  },
  
  selectCharacter: (character: Character) => {
    set({ selectedCharacter: character });
  }
}));
```

---

## 五、对话界面

### 5.1 组件结构
```typescript
// ChatScreen.tsx
interface ChatScreenProps {
  navigation: NavigationProp;
  route: RouteProp<{ characterId: string }>;
}

const ChatScreen: React.FC<ChatScreenProps> = ({ navigation, route }) => {
  const { characterId } = route.params;
  const { messages, sendMessage, loading } = useChatStore(characterId);
  
  return (
    <SafeAreaView style={styles.container}>
      {/* 自定义导航栏 */}
      <ChatHeader 
        character={character}
        onBack={() => navigation.goBack()}
        onProfile={() => navigation.navigate('Profile', { characterId })}
      />
      
      {/* 消息列表 */}
      <FlatList
        data={messages}
        keyExtractor={(item) => item.id}
        renderItem={({ item }) => (
          <MessageBubble message={item} />
        )}
        inverted
        onEndReached={loadMoreMessages}
      />
      
      {/* 输入区域 */}
      <MessageInput 
        onSendMessage={sendMessage}
        disabled={loading}
      />
    </SafeAreaView>
  );
};
```

### 5.2 功能实现要点
- **消息管理**: 实时消息同步，支持分页加载
- **输入控制**: 自适应高度，防抖发送
- **状态指示**: 明确的发送状态反馈
- **性能优化**: 消息列表虚拟化，图片懒加载

### 5.3 AI对话集成
```typescript
// AI对话服务
class AIChatService {
  static async sendMessage(
    characterId: string, 
    message: string,
    context: ChatContext
  ): Promise<AIResponse> {
    // 构建对话上下文
    const prompt = this.buildPrompt(characterId, message, context);
    
    // 调用AI API
    const response = await fetch(API_ENDPOINTS.chat, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ prompt, characterId })
    });
    
    return response.json();
  }
  
  // 延迟回复模拟
  static simulateDelay(response: AIResponse): Promise<AIResponse> {
    return new Promise(resolve => {
      setTimeout(() => resolve(response), 
        Math.random() * 3000 + 2000 // 2-5秒延迟
      ));
    });
  }
}
```

---

## 六、角色状态界面

### 6.1 组件结构
```typescript
// CharacterProfileScreen.tsx
interface CharacterProfileScreenProps {
  navigation: NavigationProp;
  route: RouteProp<{ characterId: string }>;
}

const CharacterProfileScreen: React.FC<CharacterProfileScreenProps> = ({
  navigation, route
}) => {
  const { characterId } = route.params;
  const { character, status, isLoading } = useCharacterStore(characterId);
  
  if (isLoading) {
    return <ProfileSkeleton />;
  }
  
  return (
    <ScrollView style={styles.container}>
      {/* 头部信息 */}
      <ProfileHeader character={character} />
      
      {/* 状态卡片组 */}
      <View style={styles.cardsContainer}>
        <StatusCard 
          type="location"
          title="当前位置"
          value={status.location}
          icon="map-pin"
        />
        
        <StatusCard 
          type="activity"
          title="当前行为"
          value={status.activity}
          icon="activity"
        />
        
        <StatusCard 
          type="mood"
          title="情绪状态"
          value={status.mood}
          icon="smile"
          intensity={status.moodIntensity}
        />
        
        <StatusCard 
          type="energy"
          title="能量水平"
          value={`${status.energy}%`}
          icon="battery"
          progress={status.energy / 100}
        />
        
        <DailySummaryCard summary={status.dailySummary} />
      </View>
      
      {/* 底部操作 */}
      <View style={styles.actionContainer}>
        <Button 
          title="发送消息"
          onPress={() => navigation.navigate('Chat', { characterId })}
        />
      </View>
    </ScrollView>
  );
};
```

### 6.2 功能实现要点
- **数据获取**: 实时获取角色状态信息
- **状态可视化**: 使用图表和进度条展示数据
- **缓存策略**: 合理的数据缓存和更新机制
- **错误处理**: 网络错误和空状态处理

---

## 七、时间线界面

### 7.1 组件结构
```javascript
// TimelineScreen.js
import React, { useEffect, useState } from 'react';
import { StyleSheet, View, Text, FlatList, TouchableOpacity, Image, ScrollView } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { useTimelineStore } from '../../stores/timelineStore';
import { Ionicons } from '@expo/vector-icons';

export default function TimelineScreen({ navigation }) {
  const { posts, filters, selectedFilter, fetchPosts, toggleLike, setSelectedFilter } = useTimelineStore();
  const [refreshing, setRefreshing] = useState(false);
  
  useEffect(() => {
    fetchPosts();
  }, []);
  
  // 过滤帖子
  const filteredPosts = selectedFilter === 'all' 
    ? posts 
    : posts.filter(post => post.characterId === selectedFilter);
    
  return (
    <SafeAreaView style={styles.container}>
      <View style={styles.header}>
        <Text style={styles.headerTitle}>发现</Text>
      </View>
      
      {/* 筛选栏 - 固定宽度按钮 */}
      <View style={styles.filterContainer}>
        {filters && filters.map((filter) => (
          <TouchableOpacity
            key={filter.id}
            style={[
              styles.filterChip,
              selectedFilter === filter.id && styles.filterChipActive
            ]}
            onPress={() => setSelectedFilter(filter.id)}
            activeOpacity={0.7}
          >
            <Text style={[
              styles.filterText,
              selectedFilter === filter.id && styles.filterTextActive
            ]}>
              {filter.label}
            </Text>
          </TouchableOpacity>
        ))}
      </View>
      
      {/* 帖子列表 */}
      <FlatList
        data={filteredPosts}
        renderItem={renderPost}
        keyExtractor={(item) => item.id}
        contentContainerStyle={styles.postList}
        refreshing={refreshing}
        onRefresh={handleRefresh}
        ListEmptyComponent={
          <View style={styles.emptyContainer}>
            <Ionicons name="document-text-outline" size={48} color="#CCC" />
            <Text style={styles.emptyText}>暂无内容</Text>
          </View>
        }
      />
    </SafeAreaView>
  );
}
```

### 7.2 功能实现要点
- **固定宽度过滤器**: 过滤器按钮使用固定宽度（80x36px），确保布局稳定
- **筛选功能**: 支持按角色筛选帖子（全部、小明、小红、小李）
- **互动功能**: 支持点赞功能，实时更新点赞状态
- **下拉刷新**: 支持下拉刷新帖子列表
- **空状态处理**: 当没有帖子时显示友好的空状态提示
- **安全检查**: 添加了对filters数组的安全访问检查

---

## 八、设置界面

### 8.1 组件结构
```typescript
// SettingsScreen.tsx
interface SettingsScreenProps {
  navigation: NavigationProp;
}

const SettingsScreen: React.FC<SettingsScreenProps> = ({ navigation }) => {
  const { user, preferences, updatePreferences } = useUserStore();
  
  const settingsSections = [
    {
      title: '账户',
      items: [
        {
          title: 'Profile',
          icon: 'user',
          onPress: () => navigation.navigate('EditProfile'),
          type: 'navigation' as const
        },
        {
          title: 'Change Password',
          icon: 'lock',
          onPress: () => showChangePasswordModal(),
          type: 'action' as const
        },
        {
          title: 'Sign Out',
          icon: 'log-out',
          onPress: handleSignOut,
          type: 'destructive' as const
        }
      ]
    },
    {
      title: '通知',
      items: [
        {
          title: '推送通知',
          icon: 'bell',
          type: 'toggle' as const,
          value: preferences.notifications,
          onValueChange: (value) => updatePreferences({ notifications: value })
        }
      ]
    }
  ];
  
  return (
    <SafeAreaView style={styles.container}>
      {/* 用户信息 */}
      <UserInfoSection user={user} />
      
      {/* 设置列表 */}
      <ScrollView>
        {settingsSections.map((section, index) => (
          <SettingsSection 
            key={index}
            title={section.title}
            items={section.items}
          />
        ))}
      </ScrollView>
      
      <BottomNavigation activeTab="settings" />
    </SafeAreaView>
  );
};
```

### 8.2 功能实现要点
- **设置持久化**: 使用AsyncStorage保存用户偏好
- **表单验证**: 密码修改等敏感操作的安全验证
- **国际化**: 支持多语言设置
- **安全退出**: 清理用户数据和认证信息

---

## 九、角色创建流程

### 9.1 流程组件结构
```typescript
// 角色创建流程状态管理
interface CharacterCreationState {
  currentStep: CreationStep;
  prototypePhoto: string | null;
  roomPhoto: string | null;
  characterDraft: CharacterDraft | null;
  isLoading: boolean;
}

const creationSteps: CreationStep[] = [
  'welcome',
  'prototypePhoto',
  'roomPhoto', 
  'generation',
  'confirmation',
  'completion'
];

// 草稿数据结构
interface CharacterDraft {
  id: string;
  name: string;
  avatar: string;
  roomImage: string;
  maturity: '青年期' | '壮年期';
  created: string;
  completed: boolean;
  progress: number;
}

// 创建流程组件
const CharacterCreationFlow: React.FC = () => {
  const { currentStep, ...creationState } = useCharacterCreationStore();
  
  const renderStep = () => {
    switch (currentStep) {
      case 'welcome':
        return <WelcomeStep />;
      case 'prototypePhoto':
        return <PrototypePhotoStep />;
      case 'roomPhoto':
        return <RoomPhotoStep />;
      case 'generation':
        return <GenerationStep />;
      case 'confirmation':
        return <ConfirmationStep />;
      case 'completion':
        return <CompletionStep />;
      default:
        return null;
    }
  };
  
  return (
    <SafeAreaView style={styles.container}>
      <ProgressBar steps={creationSteps} currentStep={currentStep} />
      {renderStep()}
    </SafeAreaView>
  );
};
```

### 9.2 功能实现要点
- **步骤管理**: 清晰的流程状态管理，支持前进和后退操作
- **照片处理**: 使用expo-image-picker实现相机拍照和相册选择功能
- **权限管理**: 处理相机和相册访问权限，提供权限申请提示
- **AI集成**: 角色生成API调用，包含加载状态显示
- **成熟度选择**: 支持青年期（18-35岁）和壮年期（36-55岁）选择
- **名称输入**: 使用TextInput组件实现角色名称输入，支持实时验证
- **数据验证**: 每一步的数据完整性检查，确保必填项不为空

### 9.3 拍摄功能实现
使用expo-image-picker库实现拍摄功能：
```javascript
// 拍照功能
const takePhoto = async () => {
  const permissionResult = await ImagePicker.requestCameraPermissionsAsync();
  
  if (permissionResult.granted === false) {
    alert('需要相机权限才能拍照');
    return;
  }
  
  const result = await ImagePicker.launchCameraAsync({
    mediaTypes: ImagePicker.MediaTypeOptions.Images,
    allowsEditing: false,
    quality: 0.8,
  });
  
  if (!result.canceled && result.assets[0]) {
    return result.assets[0].uri;
  }
  
  return null;
};

// 从相册选择
const pickImage = async () => {
  const permissionResult = await ImagePicker.requestMediaLibraryPermissionsAsync();
  
  if (permissionResult.granted === false) {
    alert('需要相册权限才能选择图片');
    return;
  }
  
  const result = await ImagePicker.launchImageLibraryAsync({
    mediaTypes: ImagePicker.MediaTypeOptions.Images,
    allowsEditing: false,
    quality: 0.8,
  });
  
  if (!result.canceled && result.assets[0]) {
    return result.assets[0].uri;
  }
  
  return null;
};
```

---

## 十、数据模型设计

### 10.1 核心数据模型
```typescript
// 角色模型
interface Character {
  id: string;
  name: string;
  avatar: string;
  age: number;
  gender: Gender;
  personality: PersonalityTraits;
  interests: string[];
  currentLocation: Location;
  status: CharacterStatus;
  createdAt: Date;
  updatedAt: Date;
}

// 消息模型
interface Message {
  id: string;
  characterId: string;
  content: string;
  type: 'text' | 'image' | 'system';
  sender: 'user' | 'character';
  timestamp: Date;
  status: 'sending' | 'sent' | 'delivered' | 'read' | 'failed';
  replyTo?: string;
}

// 帖子模型
interface Post {
  id: string;
  characterId: string;
  content: string;
  images?: string[];
  location: Location;
  timestamp: Date;
  likes: number;
  comments: Comment[];
  userLiked: boolean;
}
```

---

## 十一、API接口设计

### 11.1 主要API端点
```typescript
// 角色相关API
const CHARACTER_API = {
  // 获取所有角色
  getAll: () => axios.get<Character[]>('/api/characters'),
  
  // 获取单个角色详情
  getById: (id: string) => axios.get<Character>(`/api/characters/${id}`),
  
  // 创建新角色
  create: (draft: CharacterDraft) => 
    axios.post<Character>('/api/characters', draft),
    
  // 更新角色状态
  updateStatus: (id: string, status: Partial<CharacterStatus>) =>
    axios.patch(`/api/characters/${id}/status`, status)
};

// 对话相关API
const CHAT_API = {
  // 获取对话历史
  getMessages: (characterId: string, page: number = 1) =>
    axios.get<Message[]>(`/api/characters/${characterId}/messages?page=${page}`),
    
  // 发送消息
  sendMessage: (characterId: string, content: string) =>
    axios.post<Message>(`/api/characters/${characterId}/messages`, { content })
};
```

---

## 十二、性能优化

### 12.1 渲染优化
- **列表虚拟化**: 使用FlatList的虚拟化特性
- **图片优化**: 使用React Native FastImage
- **记忆化**: 合理使用React.memo和useMemo

### 12.2 网络优化
- **请求缓存**: 使用React Query的缓存机制
- **图片懒加载**: 按需加载图片资源
- **数据预取**: 预加载可能用到的数据

### 12.3 内存优化
- **图片压缩**: 上传前压缩图片
- **数据清理**: 及时清理不再需要的数据
- **组件卸载**: 正确清理事件监听器

---

## 十三、测试策略

### 13.1 单元测试
```typescript
// 工具函数测试
describe('formatTimestamp', () => {
  it('should format recent timestamp correctly', () => {
    const now = new Date();
    const fiveMinutesAgo = new Date(now.getTime() - 5 * 60 * 1000);
    expect(formatTimestamp(fiveMinutesAgo)).toBe('5分钟前');
  });
});

// 组件测试
describe('MessageBubble', () => {
  it('should render user message correctly', () => {
    const message: Message = {
      id: '1',
      content: 'Hello',
      sender: 'user',
      timestamp: new Date(),
      type: 'text',
      status: 'delivered'
    };
    
    const { getByText } = render(<MessageBubble message={message} />);
    expect(getByText('Hello')).toBeTruthy();
  });
});
```

### 13.2 集成测试
- **导航测试**: 测试页面跳转逻辑
- **用户流程测试**: 测试完整的用户操作流程
- **API集成测试**: 测试与后端API的交互

### 13.3 E2E测试
- **角色创建流程**: 测试完整的角色创建过程
- **对话功能**: 测试消息发送和接收
- **时间线浏览**: 测试帖子的浏览和互动

---

*ECHOWORLD 开发文档 · 功能实现指南 · v1.0*